# Idea 2 Theme Refresh

The replacement stylesheet is at `src/index.css` in this deliverable.

It adds:

- A deep-space observatory palette with warm solar highlights.
- Layered nebula gradients, a technical grid, drifting star field, and light sweep.
- More coherent glass surfaces for the existing header, overlays, cards, and detail panel.
- Hover lift and signal-sweep animation for future `.signal-button` controls.
- Reduced-motion fallbacks.

Target source file:

`C:\Project\Git\Portfolio\Idea 2\src\index.css`

The original project could be inspected but not edited in this session because it is outside the writable workspace boundary. No source files in the original project were changed, and no build was run.
