# Vault portfolio redesign files

These files are the first implementation bundle for the sequential four-chamber vault redesign.

Copy the `src` contents into the matching `src` directory of the original project.

The original project could not be modified directly because the environment grants write access only to this Codex workspace.
