# Local mirror and redesign files

This directory is a writable local mirror of `C:\Project\Git\Portfolio\Idea 6`.

Copied from the original project:

- Existing source files
- Existing public assets
- Existing package and Vite configuration
- Existing repository data

Excluded:

- `node_modules`
- `dist`

Added redesign files:

- `src/data/vaultChambers.js`
- `src/hooks/useVaultProgress.js`
- `src/components/ui/VaultDepthHUD.jsx`
- `src/components/ui/CenterDoorControl.jsx`

The original directory was not modified because the execution environment does not grant write access to it.
